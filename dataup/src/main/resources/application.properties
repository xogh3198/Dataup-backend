# 어플리케이션 이름
spring.application.name=dataup

# 서버 설정
# 0.0.0.0으로 바인딩하여 모든 네트워크 인터페이스에서 접근 가능하도록 설정
# (실제 기기에서 테스트할 때 필요)
server.address=0.0.0.0
server.port=8080

# --- 추가 사항: H2 데이터베이스 설정 ---
# H2 콘솔 활성화 (개발 중 http://localhost:8080/h2-console 에서 DB 확인 가능)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# 인메모리 DB (testdb) 사용
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# --- 추가 사항: JPA 설정 ---
# 어플리케이션 실행 시 Entity를 기반으로 DB 테이블 자동 생성 (create-drop: 매번 초기화)
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true 
# SQL 쿼리 로그 표시
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

# TODO: PostgreSQL 로 변경 시 아래 주석을 해제하고 H2 설정을 주석 처리하세요.
# spring.datasource.url=jdbc:postgresql://localhost:5432/[DB_NAME]
# spring.datasource.username=[YOUR_USERNAME]
# spring.datasource.password=[YOUR_PASSWORD]
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.hibernate.ddl-auto=update # (개발 중) 또는 validate (운영)